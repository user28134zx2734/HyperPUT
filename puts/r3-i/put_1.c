#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#include <string.h>
#include <errno.h>
#include <limits.h>
int main(int argc, char* argv[]) {
int N = 1 ; if (argc > N) { long long int i11=atoll(argv[1]); long long int i21=1; if (i11==i21) { assert(0==1); } } else { printf("Error: invalid number of arguments"); } return 0;}
